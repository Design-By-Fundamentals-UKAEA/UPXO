version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  jobs:
    pre_build:
      - python -m sphinx.ext.apidoc -f -o docs/docs/srcAPIdocs src/upxo --implicit-namespaces

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: docs/requirements.txt
