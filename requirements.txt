# UPXO dependency lists — keep in sync with pyproject.toml.
# Preferred install:  pip install upxo
#                     pip install -e ".[all]"   (from a clone)
#
# This file is for environments that still use `pip install -r requirements.txt`.
# It is NOT a substitute for the package extras model.

# --- Core (same as pyproject [project] dependencies) ---
numpy>=2.2.6
scipy>=1.16.2
matplotlib>=3.10.6
pandas>=2.3.3
numba>=0.62.1
networkx>=3.5
pyvista[jupyter]>=0.46.3
scikit-image>=0.25.2
scikit-learn>=1.7.2
shapely>=2.1.1
tqdm>=4.67.1
seaborn>=0.13.2
colorama>=0.4.6
termcolor>=2.4.0
connected-components-3d>=3.26.1
xlrd>=2.0.1
openpyxl>=3.1.0
ipywidgets>=8.0.0
Pillow>=10.0.0

# --- Optional extras (uncomment or: pip install upxo[all]) ---
# viz
# plotly>=5.0.0
# mesh
# pyvoro>=1.3.2
# tetgen>=0.8.2
# io
# rasterio>=1.4.3
# ebsd
# defdap==0.93.6
